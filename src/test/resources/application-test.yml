spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/test_db
    username: cluser
    password: cluser
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        format_sql: true
